# Sudo Permission
sudo: required

# Services (Docker CLI)
services:
  - docker

# Build docker image
before_install:
  - docker build -t sathishcodes/docker-react -f Docker.dev .

# Run Test Suite
script:
  - docker run CI=true sathishcodes/docker-react npm run test
